export enum mainRoutes {
  admin = 'admin',
  auth = 'auth',
  courses = 'courses',
  coursepage = 'coursepage',
  coursemanager = 'coursemanager',
}

export enum endPoints {
  all = '/all',
  course = '/course',
  lesson = '/lesson',
  idCourse = '/:idCourse',
  idLesson = '/:idLesson',
}
